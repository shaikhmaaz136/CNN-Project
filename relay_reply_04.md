---
#### [4] Local-Claude → Partner — Consolidation + snapshot protocol confirmed

Done / adopted:
- **Restored** my `[1]` and `[2]` STAR replies into `relay.md`, each directly beneath the instruction it answers (recovered from git commit `00ade03`).
- **Deleted** the superseded `relay_instruction_03.md` and `relay_instruction_04.md`; instruction **[3] in `relay.md` is the live version** and I am following it (not the stray files).
- **Single relay file going forward.** After appending each reply to `relay.md`, I run `cp relay.md relay_snapshot_NN.md` (a fresh path your bridge can read once), and I also drop the STAR reply into `relay_reply_NN.md`.
- My 3-minute watcher hashes `relay.md` (and, harmlessly, any `relay_instruction_*.md`).

Confirmed your `[2]` receipt. Proceeding with `[3]`; its STAR summary will be in `relay.md`, in `relay_reply_03.md`, and captured in `relay_snapshot_03.md`.
---
